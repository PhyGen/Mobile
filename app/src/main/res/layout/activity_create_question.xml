<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- ProgressBar -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyle"
        android:layout_centerInParent="true"
        android:visibility="gone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!-- EditText cho nội dung câu hỏi -->
    <EditText
        android:id="@+id/edtContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:hint="Nội dung câu hỏi"
        android:inputType="textMultiLine"
        android:minHeight="100dp" />

    <!-- EditText cho nguồn câu hỏi -->
    <EditText
        android:id="@+id/edtQuestionSource"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/edtContent"
        android:layout_marginTop="16dp"
        android:hint="Nguồn câu hỏi"
        android:inputType="text" />

    <!-- EditText cho Lesson ID -->
    <EditText
        android:id="@+id/edtLessonId"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/edtQuestionSource"
        android:layout_marginTop="16dp"
        android:hint="Lesson ID"
        android:inputType="number" />

    <!-- EditText cho Question ID (để xóa) -->
    <EditText
        android:id="@+id/edtQuestionId"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/edtLessonId"
        android:layout_marginTop="16dp"
        android:hint="Question ID (để xóa)"
        android:inputType="number" />

    <!-- Button Tạo Câu Hỏi -->
    <Button
        android:id="@+id/btnCreateQuestion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/edtQuestionId"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Tạo Câu Hỏi" />

    <!-- Button Xóa Câu Hỏi -->
    <Button
        android:id="@+id/btnDeleteQuestion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnCreateQuestion"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Xóa Câu Hỏi" />

</RelativeLayout>